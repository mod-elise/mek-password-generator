# mek-password-generator
A password generator project

1. Clone repository
2. Install pytest with 
`pip install pytest`
 and run tests with python -m pytest     
3. Run application with `python -m password_generator.main`